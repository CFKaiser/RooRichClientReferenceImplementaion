package de.viadee.roo.sep.rooswingcomponents;

import java.util.List;
import java.util.logging.Logger;

public class RooLocationTable {

    private Logger log;

//    public RooLocationTable(T entity) {
////        log = Logger.getLogger(getClass().getName());
////        java.util.List entityList = getEntityList(entity);
////        java.lang.Object[][] tableData = convertToObjectArray(entityList);
////        java.lang.String[] columnNames = getColumnNames(entity);
////        this.setModel(new RooTableModel(tableData, columnNames));
//    }

    public List getEntityList() {

        return null;
    }
}
